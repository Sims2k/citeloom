# citeloom Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-10-30

## Active Technologies
- Python 3.12.x (pinned in `.python-version`, managed via pyenv) (002-chunk-retrieval)
- Python 3.12.x via pyenv (as per constitution) (005-zotero-improvements)

- Python 3.12.x (pyenv pinned) + Docling, Qdrant client, FastEmbed (sentence-transformers/all-MiniLM-L6-v2), optional OpenAI embeddings, pytest, mypy, ruff (001-project-spec)

## Project Structure

```text
src/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.12.x (pyenv pinned): Follow standard conventions

## Recent Changes
- 005-zotero-improvements: Added Python 3.12.x via pyenv (as per constitution)
- 003-framework-implementation: Added Python 3.12.x (pinned in `.python-version`, managed via pyenv)
- 002-chunk-retrieval: Added Python 3.12.x (pinned in `.python-version`, managed via pyenv)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
